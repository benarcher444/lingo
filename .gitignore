node_modules/
dist/

# SQLite database and its WAL sidecars — this is your real learning history.
# Back it up, but never commit it.
data/
*.db
*.db-shm
*.db-wal

# Secrets
.env
.env.*
!.env.example

# Generated review screenshots
screenshots/

# The archived Python app is reference only; keep it out of the new history.
old_code_repo/

# OS / editor noise
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Who may sign up. Personal email addresses, and this repository is public.
# The template is allowed_emails.example.csv.
allowed_emails.csv

# npm cache when installing as the server's lingo user (home is /opt/lingo)
.npm/
